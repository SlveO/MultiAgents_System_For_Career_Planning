# 项目进度

更新时间：2026-08-19

当前阶段：四人第 1 周成果以 `origin/integration/week1-results` 作为统一协作基线。负责人已手工执行一次 DeepSeek CLI 并生成有效的脱敏云端日志；API/Web 与完整 GPU 验证尚未执行。本次发布不包含 `main` 合并，后续需单独审查和授权。

## 总体计划

| 阶段 | 目标 | 完成标准 | 状态 |
|---|---|---|---|
| 0. 范围冻结 | 明确结题 MVP、统一目录、依赖和隐私规则 | README、接口和验收标准一致 | 已完成 |
| 1. 结题 MVP | 跑通文本/文档、追问、画像、检索、规划、反馈和日志 | 50+ 岗位、四组离线对照、测试通过 | 已完成 |
| 2. 团队整合 | 将四人成果收敛为一套代码和数据 | 不保留平行 `src/`，接口与测试统一 | 已完成，待审阅 |
| 3. 真实证据 | 验证一次 DeepSeek、API/Web 和必要 GPU 冒烟流程 | 有脱敏日志和可复现环境记录 | 进行中：CLI 已完成 |
| 4. 主研究实验 | 比较单体多模态、一次性模块化、多轮模块化系统 | 固定案例、60 次受控运行、盲评和报告 | 协议已冻结，待实现与准备案例 |

## 最终产出

- 可安装、可演示的文本/文档职业规划 MVP，以及可选 API/Web/GPU 配置；
- 统一数据结构、65 条职业知识、匿名案例和脱敏运行日志；
- 可重复执行并输出 CSV/JSON 的结题消融实验；
- 固定的主实验案例、模型与环境记录、人工评分表和对比报告；
- 持续维护的架构、接口、验证和中英文进度文档。

## 四人交接清单

| 负责人 | 已整合交付物 | 期限或依赖 | 验收标准 | 下一步动作 |
|---|---|---|---|---|
| 负责人 | 结题 MVP、隐私、实验、仓库清理和最终整合 | 2026-08-21 前收到成员分支 | 全部测试通过，无密钥和运行数据 | 评分契约已完成；等待审查三个成员分支 |
| 成员 A | CLI/会话/日志骨架与冒烟流程 | 已冻结的实验协议 | 阶段事件已脱敏并有端到端和日志测试 | 实现离线架构实验运行器和有限轮次控制器 |
| 成员 B | DeepSeek 基线、结构化错误和重试测试 | 已授权的 L20 使用条件 | 类型化错误与重试测试保留；Windows 不下载研究模型 | 记录 L20 环境并准备本地模型适配器 |
| 成员 C | 20 条岗位、追问规则、3 个检索案例 | 公开来源及可再分发案例素材 | 65 个岗位无重复，3 个目标岗位检索通过 | 核验来源 URL 并交付 6 个预实验案例 |

成员原始文件保留在本地且已忽略的 `corwork/` 中。有效逻辑已移植到 `project/` 和 `dataset/`；旧 `src/`、pytest 缓存、原始日志、重复依赖和临时计划文档没有进入正式仓库。

## 两天执行计划：2026-08-20 至 2026-08-21

所有人先执行 `git fetch origin`，再从 `origin/integration/week1-results`
创建下表中的个人分支。不得直接向共享整合分支并发推送任务提交。

| 负责人 / 分支 | 第 1 天：2026-08-20 | 第 2 天：2026-08-21 | 交付物 | 验收与下一步 |
|---|---|---|---|---|
| 负责人 / `work/lead-evaluation` | 冻结案例清单字段、统一提示词、输出结构和五维 1-5 分评分表 | 审查 A/B/C 分支，复跑检查并记录接受或修改意见 | `docs/evaluation-rubric.md` 和整合审查记录 | 每项指标有评分锚点和分歧处理规则；不合并未完成内容 |
| 成员 A / `work/member-a-experiment-runner` | 增加不依赖模型的实验数据契约和三组假适配器 | 增加最多 2/3 轮控制器、离线运行器和 `unittest` | `project/experiments/architecture_protocol.py`、`run_architecture_experiments.py` 及针对性测试 | 无 `torch` 也能导入；假运行输出三组；轮次上限和失败降级测试通过；推送分支供审查 |
| 成员 B / `work/member-b-l20-models` | 仅在 L20 记录 Ubuntu、驱动、CUDA、Python、PyTorch、磁盘、网络和显存 | 增加可复现下载与适配脚本，在 L20 下载三个冻结模型并分别完成一次确定性冒烟推理 | `scripts/models/download_research_models.py`、本地模型适配器和脱敏的 `docs/l20-setup.md` | 记录模型修订和峰值显存；不得记录主机名、账号、令牌、私人路径，不得在 Windows 下载；推送代码/文档分支 |
| 成员 C / `work/member-c-pilot-cases` | 核验新增 20 条职业记录的来源 URL，并选择有许可或自制的预实验素材 | 完成 3 个图像和 3 个 PDF 页面案例，包含固定画像、知识 ID、评分说明及每例 3-5 个预期事实 | 版本化来源元数据和 `dataset/research_cases/` 清单、素材、README | 6 个案例可解析、无身份信息且含来源/许可字段；推送分支供审查 |

如果 L20 权限或依赖阻塞任务，负责人必须记录准确命令、错误、已完成证据和下一步动作，不得用未验证结果替代。训练、音频、完整 20 案例运行和直接修改 `main` 均不在这两天范围内。

### 负责人执行状态

第 1 天任务已在本地分支 `work/lead-evaluation` 完成：案例、证据、决策和
规划 Schema，带哈希的提示词/运行协议，五维评分量表、轮次选择规则、长期
决策记录和整合审查清单均已写入，并由 11 项契约测试覆盖。第 2 天审查需要
等待成员 A/B/C 推送可审查分支；收到后按
`docs/integration-review-checklist.md` 分别记录“接受/修改后再审/阻塞”，不把
未通过内容并入共享分支。

## 当前验证结果

- 默认 `.venv`：发现 73 项，71 项通过；2 项 API 测试因核心依赖不安装 FastAPI 而跳过。
- 已有 `agents` Conda 环境：73 项全部通过，包括 API 集成测试。
- 四个 Draft 2020-12 研究 Schema 均在 `agents` 环境通过 `jsonschema` 元模式校验。
- `compileall`、CLI 帮助、`git diff --check`：通过。
- 离线四组实验：成功输出 8 行 JSON 和 CSV。
- 负责人真实 API 记录：2026-08-18 18:45 UTC 的日志为 `deepseek-v4-flash`、`cloud_brain`、`completed`，包含 7 个阶段事件。扫描未发现原始路径/内容字段或已配置的身份信息、API Key 模式。本次输入没有可识别身份信息，因此没有出现脱敏占位符；人工构造的脱敏测试仍通过。
- 按约定未执行：完整 Qwen/Whisper/BGE 加载、L20 批量实验、前端构建（当前无 `web/node_modules`）。

## 已冻结的主研究方案

- 三组模型：`Qwen3-VL-8B-Instruct` 单体组；
  `Qwen3-VL-2B-Instruct` 感知后交给 `Qwen3-4B-Instruct-2507` 的一次性模块化组；
  使用相同 2B/4B 模型并允许有限证据澄清的协作组。
- 案例：10 个图像和 10 个 PDF 页面，每例标注 3 至 5 个预期证据事实；决定性证据不在固定文本画像中重复。
- 预实验：使用相同 6 个案例分别设置最多 2 轮和 3 轮，随后为 20 案例主实验冻结一个轮次上限。后续再单独比较 `0/1/2/3` 轮。
- 运行参数：贪心解码、最多 2,048 个输出 token、BF16、PDF 144 DPI、视觉 token 256–1,280；精确模型修订仍需在 L20 冻结。
- 执行环境：主研究模型只在可联网、48 GB 显存的 L20 Ubuntu 主机下载和运行。安装前先记录实际 Ubuntu 版本、驱动、CUDA、Python、PyTorch 和磁盘状态；当前 Windows 机器不下载主研究权重。
- 当前状态：实验策略和机器可读契约已完成，已确认及后续替代决策保存在 `docs/research-decisions.md`；实验适配器、20 案例清单、模型下载、真实推理和人工评分均尚未完成。

## 下一阶段任务

1. 所有人从 `origin/integration/week1-results` 创建个人分支，完成两天交付物，不直接写共享分支。
2. 负责人在 2026-08-21 检查点后审查三个分支，仅整合通过验收和测试的文件。
3. 6 案例预实验完成后冻结一个协作轮次上限，再进入 60 次主对照；第一轮不加入训练与音频。
