# Documentation Map

- `architecture.md`: current MVP architecture and the target modular-agent
  research architecture.
- `interfaces.md`: module boundaries, inputs, outputs, and optional-feature
  failure behavior.
- `experiments.md`: primary architecture comparison and secondary career
  planning ablations.
- `progress.md`: owner, deliverable, dependency, acceptance criterion, and
  next action for each completion task.

The old `docs/superpowers/` implementation plan was a temporary execution
record. Current decisions belong in the documents above.
